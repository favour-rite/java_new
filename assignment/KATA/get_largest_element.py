def LargestElement(numbers:list):
	largest = list[0]
	for number in numbers:
		if largest < number:
			largest = number
	print(largest)

def reverse(number):
	for count in number.length-1; count >= 0; count--
		
		print(number[count] + " ")
	
	print()
def occuring(number):
	for(int count = 0; count < numbers.length; count++):
		if (numbers[count] == target):
			print(true)
		
		else:
			print(false)

def  oddposition(number):
	
	


	